# Prompts

Versioned prompt files for L4 adjudication (§4.4). Empty until gate 11.

Every prompt file is versioned and its version is logged with each verdict, so
the audit trail can answer: *who decided this, on what evidence, when, under
which prompt version* (§9.3).
